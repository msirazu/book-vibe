import Navbar from "./Navbar";

const Header = () => {
    return (
        <div>
            <section>

            </section>
            <section id="navbar-section">
            <Navbar/>
            </section>
        </div>
    );
};

export default Header;